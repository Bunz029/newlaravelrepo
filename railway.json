{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x railway-build.sh && ./railway-build.sh"
  },
  "deploy": {
    "startCommand": "php artisan storage:link && php -d upload_max_filesize=20M -d post_max_size=25M -d memory_limit=256M -d max_execution_time=300 -S 0.0.0.0:$PORT -t public",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
